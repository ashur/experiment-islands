{%- from "components/stopwatch/stopwatch.njk" import render as Stopwatch with context -%}

{#
 # @param {Object} params
 #}
{% macro render(params) %}
	{{ Stopwatch(
		id = params.id,
		name = params.name
	) }}
{% endmacro %}

{% if storybookArgs %}
	{{ render(storybookArgs) }}
{% endif %}
