const path = require( "path" );

module.exports.pairedShortcodes = {
	// "example": path.join(__dirname, "example.js"),
};

module.exports.shortcodes = {
	"addDependency": path.join( __dirname, "add-dependency.js" ),
};
